import RegisterUser from '../../components/auth/RegisterUser';
import './auth.scss';

const Register = () => {
	return (
		<div className='auth-page'>
			<RegisterUser />
		</div>
	);
};

export default Register;
